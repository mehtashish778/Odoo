// For now, we'll use an in-memory array to store users.
// Later, this will be replaced with a database model.
const users = [];

// We'll also need bcrypt for password hashing later
// const bcrypt = require('bcryptjs'); 

module.exports = users; 